import { ApiConsoleLayout } from "@/features/enhe-api/components/console-layout";

export default function UserApiLayout({ children }: { children: React.ReactNode }) {
  return <ApiConsoleLayout>{children}</ApiConsoleLayout>;
}
